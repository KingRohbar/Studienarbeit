<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.kingrohbar.leavethehouse">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeaveTheHouse">
        <activity
            android:name=".activities.CreateTask"
            android:label="Create Task"
            android:parentActivityName=".activities.OpenChecklistActivity"
            android:theme="@style/Theme.LeaveTheHouse.NoActionBar" />
        <activity
            android:name=".activities.OpenChecklistActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".activities.CreateChecklist"
            android:label="@string/title_activity_create_checklist"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.LeaveTheHouse.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.LeaveTheHouse.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>